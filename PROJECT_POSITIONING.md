# 项目定位

## 一句话定位

这是一个面向 New API 使用者的多站点、多模态模型自动适配与统一调用 Hub。

用户只需添加 New API 实例的地址和 API Key，Hub 即可发现其中的模型，识别 LLM、图片、视频和音频能力，标准化参数，验证兼容性，并为软件或网站提供统一调用接口。

## 产品本质

本项目不是 New API 的替代品，而是位于应用与多个 New API 实例之间的模型兼容层：

```text
软件或网站 -> 本项目 Hub -> 多个 New API 实例 -> 上游模型供应商
```

Hub 同时包含两部分：

- **模型控制中心**：管理模型身份、版本、能力、参数、来源、供应商绑定和模型变体。
- **模型调用网关**：将统一请求转换为具体 New API 站点和模型所需的请求，并处理路由、重试、降级和异步任务。

## 目标用户

- 需要在软件或网站中接入多个 AI 模型的开发团队。
- 需要同时使用多个 New API 站点的个人开发者和企业。
- 不希望逐个研究模型参数和供应商接口差异的产品团队。
- 需要限制模型能力、封装业务版本和快速替换上游站点的团队。

## 解决的问题

不同 New API 站点中的模型通常存在以下差异：

- 模型名称和实际来源不同。
- 参数名称、类型、默认值和限制不同。
- LLM、图片、视频和音频接口的调用生命周期不同。
- 同一模型在不同站点上的能力和配额不同。
- 有些模型支持同步或流式调用，有些模型需要异步任务和 Webhook。

本项目将这些差异收敛到 Hub 内部，使上层软件只依赖稳定的统一接口。

## 核心工作链路

```text
添加 New API 实例
-> 发现模型
-> 识别模型能力
-> 标准化参数
-> 验证兼容性
-> 创建模型变体
-> 绑定站点和模型
-> 通过统一接口调用
```

## 模型能力范围

### LLM

聊天、推理、结构化输出、工具调用、Embedding 和多模态输入。

### 图片

文生图、图生图、图片编辑、图片理解、蒙版、参考图和放大修复。

### 视频

文生视频、图生视频、视频编辑、参考视频、时长、分辨率和异步任务。

### 音频

文本转语音、语音转文本、语音转换、音乐生成、音效生成和参考音频。

多模态不是完全独立的目录，而是模型的跨模态能力标签。一个模型可以同时属于 LLM、图片理解和音频输入等多个能力集合。

## 模型变体

模型变体是在原始模型之上建立的业务适配版本，不修改原始模型。变体可以定义：

- 参考图片、音频和视频的数量上限。
- 最大分辨率、时长、上下文长度和输出数量。
- 默认参数、禁止参数和供应商字段映射。
- 供应商优先级、降级策略和适用场景。

## 产品边界

本项目不是：

- 单纯的模型资料展示网站。
- 单纯的 New API 聚合网关。
- 模型训练平台。
- 只面向 LLM 的聊天代理。
- 初期的模型交易市场或计费平台。

## 核心差异化

现有项目通常只覆盖模型目录、模型供应商管理或统一网关中的一部分。本项目的差异化在于：

> 添加一个 New API 地址和 Key 后，自动发现站点中的模型，判断其 LLM、图片、视频和音频能力，建立参数适配关系，并立即生成可供软件或网站使用的统一调用能力。

## 对外描述

一个面向 New API 使用者的多站点、多模态模型自动适配平台。开发者只需添加 New API 地址和 Key，Hub 就能自动发现并标准化其中的 LLM、图片、视频和音频模型，让软件通过统一接口快速调用不同站点和不同品牌的模型。
